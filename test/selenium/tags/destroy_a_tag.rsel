load File.join(RAILS_ROOT, 'test', 'selenium_helper.rb')

setup :fixtures => [:users]

Tag.delete_all
tag = Tag.create! valid_tag_attributes(:user_id => User.find_by_login("quentin").id, :public => true)

login
open "/tags"

see_element "#destroy_tag_#{tag.id}"
click_and_wait "destroy_tag_#{tag.id}"
assert_confirmation "*"
dont_see_element "#destroy_tag_#{tag.id}"
