if @full
  page.call 'itemBrowser.setFull', true
end
@feed_items.each do |feed_item|
  page.call 'itemBrowser.insertItem', dom_id(feed_item), render_to_string(:partial => 'feed_item.html.erb', :object => feed_item)
end
