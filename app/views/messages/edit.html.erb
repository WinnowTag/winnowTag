<h1><%= t("winnow.messages.edit.editing_message") %></h1>

<%= error_messages_for :message %>

<% form_for(@message) do |f| %>
  <p>
    <%= f.text_area :body %>
  </p>

  <p>
    <%= f.submit t("winnow.general.update") %>
  </p>
<% end %>

<%= link_to t("winnow.general.back"), messages_path %>
