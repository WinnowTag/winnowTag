<h1><%= _(:messages_new_message) %></h1>

<%= error_messages_for :message %>

<% form_for(@message) do |f| %>
  <p>
    <b>Body</b><br />
    <%= f.text_area :body %>
  </p>

  <p>
    <%= f.submit _(:create) %>
  </p>
<% end %>

<%= link_to _(:back), messages_path %>
