<%= {
  :id => dom_id(@tag),
  :prompt_to_delete_tag => @tag.taggings.empty?,
  :prompt_to_delete_tag_message => t("winnow.taggings.destroy_message", :tag => @tag.name),
  :prompt_to_delete_tag_url => tag_path(@tag),
  :tooltip => tag_tooltip(@tag),
  :classifierProgress => t("winnow.taggings.classifier_progress")
}.to_json %>
