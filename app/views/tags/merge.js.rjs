page << <<-EOJS
  if(confirm("This will merge #{@tag.name} with #{@name}. Are you sure you want to do this?")) {
    #{remote_function(:url => hash_for_merge_tag_path(:id => @tag, "tag[name]" => @name), :method => :put)};
  }
EOJS