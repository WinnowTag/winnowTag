page['tag_filters'].insertInOrder '.name@data-sort', tag_filter_control(@tag, :editable => true, :remove => :subscription), @tag.sort_name
page << "itemBrowser.bindTagFilterEvents($('tag_filters').down('##{dom_id(@tag)}'));"
