<%= {
  :full => @full,
  :items => @tags.map do |tag|
    { :id => dom_id(tag), 
      :content => render(:partial => tag.user == current_user ? 'tag.html.erb' : 'public_tag.html.erb', :object => tag) }
  end
}.to_json %>