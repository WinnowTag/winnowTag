page << <<-EOJS
  new ConfirmationMessage(#{t(:tags_sidebar_confirm_message, :tag => @tag.name).to_json}, function() {
    #{remote_function(:url => tag_path(@tag), :method => :delete)}
  });
EOJS

page.select(".#{dom_id(@tag)}").invoke("remove")
