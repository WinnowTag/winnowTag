page << <<-EOJS
  new ConfirmationMessage(#{_(:tags_sidebar_confirm_message, @tag.name, 0, 0, @tag.name, @tag.name).to_json}, {
    yes: #{_(:tags_destroy_tag, @tag.name).to_json},
    no: #{_(:tags_remove_tag, @tag.name).to_json},
    onConfirmed: function() {
      #{remote_function(:url => tag_path(@tag), :method => :delete)}
    }
  });
EOJS

page.select(".#{dom_id(@tag)}").invoke("remove")
