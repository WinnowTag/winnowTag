page << "if($('#{dom_id(@tag)}')) {"
  page[@tag].remove
page << "}"

page.select(".#{dom_id(@tag)}").invoke("remove")

page << "Message.add('notice', #{t(:tags_unsubscribe_text, :tag => h(@tag.name)).to_json});"