page << "if($('#{dom_id(@tag)}')) {"
  page[@tag].remove
page << "}"

page.select(".#{dom_id(@tag)}").invoke("remove")

page << "new NoticeMessage(" + _(:tags_unsubscribe_text, @tag.name).to_json + ");"