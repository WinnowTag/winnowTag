page << "if(itemBrowser) { itemBrowser.removeTag(#{@tag.name.to_json}); }"
page << "if($('#{dom_id(@tag)}')) {"
  page[@tag].remove
page << "}"

page << "Message.add('notice', #{"Deleted #{@tag.name}".to_json});"
