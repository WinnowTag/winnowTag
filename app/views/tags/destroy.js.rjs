page << "if(itemBrowser) { itemBrowser.removeTag(#{@tag.name.to_json}); }"
page[:notice].update "Deleted #{@tag.name}"
page[:notice].show
page << "if($('#{dom_id(@tag)}')) {"
  page[@tag].remove
page << "}"